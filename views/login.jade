extends layout

block page
    - var menu = 'login'

block content
    .col-md-4.col-md-offset-4
    h2 Login
    div= error
    div
        form(action='/login', method='post')
            p
                input.form-control(name='email', type='text', placeholder='admin@fhf.com')
            P
                input.form-control(name='password', type='password', placeholder='********')
                